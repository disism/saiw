import { MusicContextDirective } from './music-context.directive';

describe('MusicContextDirective', () => {
  it('should create an instance', () => {
    const directive = new MusicContextDirective();
    expect(directive).toBeTruthy();
  });
});
